#!/bin/bash

#set -x

if [[ $# > 1 || $# = 1 && "$1" = -* ]]; then
    /bin/ls "$@"
    exit
fi

d="$1"
[ -z $1 ] && d=.

f=`ls -1AF "$d"`
[ -z "$f" ] && exit

echo "$f" | grep '.*/$'
echo "$f" | grep -v '.*/$'
